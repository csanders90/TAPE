method: bayes
metric:
  goal: maximize
  name: score
parameters:
  wl:
    max: 15
    min: 5
    distribution: int_uniform
  num_walks:
    values: [10, 20, 30]
  ws:
    values: [2, 3, 4, 5, 6]

# https://wandb.ai/graph-diffusion-model-link-prediction/deepwalk-sweep-pubmed/sweeps/1fygbzox?nw=nwusercshao676